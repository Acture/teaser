[package]
name = "teaser-core"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
schemars = { version = "1.2.1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
